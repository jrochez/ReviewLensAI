// Placeholder — CSP is set via static headers in next.config.js.
// A nonce-based CSP requires Next.js to stamp the nonce onto its own RSC
// streaming inline scripts, which needs deeper framework integration than
// middleware alone can provide with the current Next.js version.
export const config = { matcher: [] }

