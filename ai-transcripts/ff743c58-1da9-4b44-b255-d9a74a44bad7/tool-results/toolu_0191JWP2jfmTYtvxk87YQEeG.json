[
  {
    "type": "text",
    "text": "Executed 12 commands (1353 lines, 53.3KB). Indexed 28 sections. Searched 10 queries.\n\n## Indexed Sections\n\n- Frontend API lib (3.9KB)\n- Frontend Auth Context (1.6KB)\n- Frontend AuthGuard (0.5KB)\n- Frontend Chat Page (1) (3.2KB)\n- Frontend Chat Page (2) (3.4KB)\n- Frontend Chat Page (3) (1.4KB)\n- Frontend Dataset Detail (1) (3.6KB)\n- Frontend Dataset Detail (2) (1.9KB)\n- Frontend Dataset Detail (3) (2.2KB)\n- Frontend Dataset Detail (4) (2.5KB)\n- Frontend Dataset Detail (5) (2.2KB)\n- Frontend Login Page (3.3KB)\n- Backend Dockerfile (0.4KB)\n- Frontend Dockerfile (0.6KB)\n- Backend entrypoint (0.2KB)\n- Backend ingest service (1) (3.8KB)\n- Backend ingest service (2) (3.8KB)\n- Backend ingest service (3) (2.2KB)\n- Backend RAG service (1) (3.9KB)\n- Backend RAG service (2) (3.7KB)\n- Backend RAG service (3) (2.8KB)\n- Env file contents (0.0KB)\n- ── Bright Data ────────────────────────────────────────────────────────────── (0.4KB)\n- ── OpenAI ─────────────────────────────────────────────────────────────────── (0.4KB)\n- ── App ────────────────────────────────────────────────────────────────────── (0.3KB)\n- ── RAG ────────────────────────────────────────────────────────────────────── (0.3KB)\n- ── Initial Admin User ─────────────────────────────────────────────────────── (0.3KB)\n- ── Security ───────────────────────────────────────────────────────────────── (0.4KB)\n\n## token localStorage sessionStorage cookie storage auth\n\n### Frontend Auth Context\n# Frontend Auth Context\n\n'use client'\n\nimport { createContext, useContext, useEffect, useState, ReactNode } from 'react'\nimport { User } from '@/types'\n\ninterface AuthContextValue {\n  token: string | null\n  user: User | null\n  login: (token: string, user: User) => void\n  logout: () => void\n  isAuthenticated: boolean\n  initialized: boolean\n}\n\nconst AuthContext = createContext<AuthContextValue | null>(null)\n\nexport function AuthProvider({ children }: { children: ReactNode }) {\n  const [token, setToken] = useState<string | null>(null)\n  const [user, setUser] = useState<User | null>(null)\n  const [initialized, setInitialized] = useState(false)\n\n  useEffect(() => {\n    const savedToken = sessionStorage.getItem('rl_token')\n    const savedUser = sessionStorage.getItem('rl_user')\n    if (savedToken && savedUser) {\n      setToken(savedToken)\n      setUser(JSON.parse(savedUser))\n    }\n    setInitialized(true)\n  }, [])\n\n  const login = (newToken: string, newUser: User) => {\n    sessionStorage.setItem('rl_token', newToken)\n    sessionStorage.setItem('rl_user', JSON.stringify(newUser))\n    setToken(newToken)\n    setUser(newUser)\n  }\n\n  const logout = () => {\n    sessionStorage.removeItem('rl_token')\n    sessionStorage.removeItem('rl_user')\n    setToken(null)\n    setUser(null)\n  }\n\n  return (\n    <AuthContext.Provider value={{ token, user, login, logout, isAuthenticated: !!token, initialized }}>\n      {children}\n    </AuthContext.Provider>\n  )\n}\n\nexport function useAuth() {\n  const ctx = useContext(AuthContext)\n  if (!ctx) throw new Error('useAuth must be used within AuthProvider')\n  return ctx\n}\n\n### Frontend API lib\n# Frontend API lib\n\nimport { Dataset, Review, ReviewsResponse, Summary, RateLimitStatus, ChatMessage } from '@/types'\n\nconst API_BASE = '/api/v1'\n\nfunction getToken(): string | null {\n  if (typeof window === 'undefined') return null\n  return sessionStorage.getItem('rl_token')\n}\n\nasync function apiFetch<T>(path: string, options: RequestInit = {}): Promise<T> {\n  const token = getToken()\n  const headers: Record<string, string> = {\n    'Content-Type': 'application/json',\n    ...(options.headers as Record<string, string> || {}),\n  }\n  if (token) headers['Authorization'] = `Bearer ${token}`\n\n  const res = await fetch(`${API_BASE}${path}`, { ...options, headers })\n\n  if (res.status === 401) {\n    sessionStorage.removeItem('rl_token')\n    sessionStorage.removeItem('rl_user')\n    window.location.href = '/login?expired=1'\n    throw new Error('Unauthorized')\n  }\n\n  if (!res.ok) {\n    const err = await res.json().catch(() => ({ message: 'Request failed' }))\n    throw Object.assign(new Error(err.message || 'Request failed'), { status: res.status, data: err })\n  }\n\n  if (res.status === 204) return undefined as T\n  return res.json()\n}\n\n// Auth\nexport const authApi = {\n  login: async (email: string, password: string) => {\n    const res = await fetch(`${API_BASE}/auth/login`, {\n      method: 'POST',\n      headers: { 'Content-Type': 'application/json' },\n      body: JSON.stringify({ email, password }),\n    })\n    if (!res.ok) {\n      const err = await res.json().catch(() => ({ message: 'Login failed' }))\n      throw Object.assign(new Error(err.message || 'Login failed'), { status: res.status, data: err })\n    }\n    return res.json() as Promise<{ token: string; expires_at: string; user: { id: string; email: string } }>\n  },\n  logout: () => apiFetch<void>('/auth/logout', { method: 'POST' }),\n}\n\n// Datasets\nexport const datasetsApi = {\n  list: () => apiFetch<{ datasets: Dataset[] }>('/datasets'),\n  get: (id: string) => apiFetch<Dataset>(`/datasets/${id}`),\n  create: (input: string) =>\n    apiFetch<{ id: string; asin: string; status: string }>('/datasets', {\n      method: 'POST',\n      body: JSON.stringify({ input }),\n    }),\n  upload: async (file: File): Promise<{ id: string; status: string }> => {\n    const token = getToken()\n    const headers: Record<string, string> = {}\n    if (token) headers['Authorization'] = `Bearer ${token}`\n    const body = new FormData()\n    body.append('file', file)\n    const res = await fetch(`${API_BASE}/datasets/upload`, { method: 'POST', headers, body })\n    if (res.status === 401) {\n      sessionStorage.removeItem('rl_token')\n      sessionStorage.removeItem('rl_user')\n      window.location.href = '/login?expired=1'\n      throw new Error('Unauthorized')\n    }\n    if (!res.ok) {\n      const err = await res.json().catch(() => ({ message: 'Upload failed' }))\n      throw Object.assign(new Error(err.message || 'Upload failed'), { st…\n\n### Frontend Login Page\n# Frontend Login Page\n\n'use client'\n\nimport { useState } from 'react'\nimport { useRouter } from 'next/navigation'\nimport { useAuth } from '@/lib/auth-context'\nimport { authApi } from '@/lib/api'\n\nexport default function LoginPage() {\n  const router = useRouter()\n  const { login } = useAuth()\n  const [email, setEmail] = useState('')\n  const [password, setPassword] = useState('')\n  const [error, setError] = useState('')\n  const [loading, setLoading] = useState(false)\n\n  const handleSubmit = async (e: React.FormEvent) => {\n    e.preventDefault()\n    setError('')\n    setLoading(true)\n    try {\n      const res = await authApi.login(email, password)\n      login(res.token, res.user)\n      router.push('/datasets')\n    } catch (err: any) {\n      setError(err?.data?.message || 'Invalid email or password.')\n    } finally {\n      setLoading(false)\n    }\n  }\n\n  return (\n    <div className=\"min-h-screen flex items-center justify-center bg-surface px-4\">\n      <div cla…\n\n## upload file size limit max content length\n\n### Frontend API lib\n…api/v1'\n\nfunction getToken(): string | null {\n  if (typeof window === 'undefined') return null\n  return sessionStorage.getItem('rl_token')\n}\n\nasync function apiFetch<T>(path: string, options: RequestInit = {}): Promise<T> {\n  const token = getToken()\n  const headers: Record<string, string> = {\n    'Content-Type': 'application/json',\n    ...(options.headers as Record<string, string> || {}),\n  }\n  if (token) headers['Authorization'] = `Bearer ${token}`\n\n  const res = await fetch(`${API_BASE}${path}`, { ...options, headers })\n\n  if (res.status === 401) {\n    sessionStorage.removeItem('rl_token')\n…\n\n…ed'), { status: res.status, data: err })\n  }\n\n  if (res.status === 204) return undefined as T\n  return res.json()\n}\n\n// Auth\nexport const authApi = {\n  login: async (email: string, password: string) => {\n    const res = await fetch(`${API_BASE}/auth/login`, {\n      method: 'POST',\n      headers: { 'Content-Type': 'application/json' },\n      body: JSON.stringify({ email, password }),\n    })\n    if (!res.ok) {\n      const err = await res.json().catch(() => ({ message: 'Login failed' }))\n      throw Object.assign(new Error(err.message || 'Login failed'), { status: res.status, data: err })\n    }\n …\n\n… {\n  list: () => apiFetch<{ datasets: Dataset[] }>('/datasets'),\n  get: (id: string) => apiFetch<Dataset>(`/datasets/${id}`),\n  create: (input: string) =>\n    apiFetch<{ id: string; asin: string; status: string }>('/datasets', {\n      method: 'POST',\n      body: JSON.stringify({ input }),\n    }),\n  upload: async (file: File): Promise<{ id: string; status: string }> => {\n    const token = getToken()\n    const headers: Record<string, string> = {}\n    if (token) headers['Authorization'] = `Bearer ${token}`\n    const body = new FormData()\n    body.append('file', file)\n    const res = await fetch(`${API_BASE}/datasets/upload`, { method: 'POST', headers, body })\n    if (res.status === 401) {\n      sessionStorage.removeItem('rl_token')\n      sessionStorage.removeItem('rl_user')\n      window.location.href = '/login?expired=1'\n      throw new Error('Unauthorized')\n    }\n    if (!res.ok) {\n      const err = await res.json().catch(() => ({ message: 'Upload failed' }))\n      throw Object.assign(new Error(err.message || 'Upload failed'), { status: res.status, data: err })\n    }\n    return res.json()\n  },\n  rescrape: (id: string) =>\n    apiFetch<{ status: string }>(`/datasets/${id}/rescrape`, { method: 'POST' }),\n  delete: (id: string) => apiFetch<void>(`/datasets/${id}`, { method: 'DELETE' }),\n}\n\n// Reviews & Summar…\n\n… Record<string, string | number>) => {\n    const qs = new URLSearchParams(Object.entries(params).map(([k, v]) => [k, String(v)])).toString()\n    return apiFetch<ReviewsResponse>(`/datasets/${id}/reviews?${qs}`)\n  },\n  summary: (id: string) => apiFetch<Summary>(`/datasets/${id}/summary`),\n}\n\n// Rate limit\nexport const scrapeApi = {\n  rateLimit: () => apiFetch<RateLimitStatus>('/scrape/rate-limit'),\n}\n\n// Chat\nexport const chatApi = {\n  send: (datasetId: string, message: st\n\n### Env file contents\n# Env file contents\n\n### Frontend Dataset Detail (2)\nreturn (\n    <AuthGuard>\n      <div className=\"flex flex-col min-h-screen\">\n        <TopNav />\n        <main className=\"flex-1 px-6 py-6 max-w-6xl mx-auto w-full\">\n          {/* Header */}\n          <div className=\"flex items-start justify-between mb-6\">\n            <div>\n              <div className=\"flex items-center gap-2 text-text-secondary text-sm mb-1\">\n                <Link href=\"/datasets\" className=\"hover:text-text-primary flex items-center gap-1\"><ArrowLeft size={14} /> Datasets</Link>\n              </div>\n              <h1 className=\"text-2xl font-semibold text-text-primary\">{ds.product_name || ds.asin}</h1>\n              <div className=\"flex items-center gap-3 mt-1\">\n                <span className=\"font-mono text-xs text-text-secondary\">ASIN: {ds.asin}</span>\n                <StatusBadge status={ds.status} />\n                {ds.last_scraped_at && <span className=\"text-xs text-text-secondary\">Last scraped {new Date(ds.last_scraped_at).toLocaleDateString()}</span>}\n              </div>\n            </div>\n            <div className=\"flex gap-2\">\n              <button onClick={() => datasetsApi.rescrape(id)} className=\"flex items-center gap-1.5 px-4 py-2 text-sm border border-border rounded-lg hover:bg-raised transition-colors\">\n                <RefreshCw size={14} /> Re-scrape\n              </button>\n              <Link href={`/datasets/${id}/chat`} className=\"flex items-center gap-1.5 px-4 py-2 text-sm bg-accent hover:bg-accent-hover text-white rounded-lg transition-colors font-medium\">\n                <MessageSquare size={14} /> Ask Questions\n              </Link>\n            </div>\n          </div>\n\n          {ds.status !== 'ready' && (\n            <div className=\"text-center py-16 text-text-secondary\">\n              {ds.status === 'error' ? <p className=\"text-error\">{ds.error_message || 'Scraping failed.'}</p> : <p>Dataset is {ds.status}. Please wait…</p>}\n            </div>\n          )}\n\n## async background task asyncio create_task fire forget\n\n### Backend ingest service (1)\n# Backend ingest service\n\nimport asyncio\nimport logging\nfrom datetime import datetime, timedelta\nfrom uuid import uuid4\n\nfrom sqlalchemy import func, select\nfrom sqlalchemy.ext.asyncio import AsyncSession\n\nfrom app.config import settings\nfrom app.db.models import Dataset, DatasetTheme, Review, ScrapeUsage\nfrom app.db.session import AsyncSessionLocal\nfrom app.security import blocklist\nfrom app.services.scraper import RawReview, get_scraper\n\nlogger = logging.getLogger(__name…\n\n…rating: int) -> str:\n    if star_rating >= 4:\n        return \"positive\"\n    if star_rating == 3:\n        return \"neutral\"\n    return \"negative\"\n\ndef _sanitize(text: str) -> tuple[str, bool]:\n    truncated = text[: 2000]\n    cleaned, detected = blocklist.scan(truncated)\n    return cleaned, detected\n\nasync def check_budgets(db: AsyncSession, user_id: str) -> tuple[bool, datetime | None]:\n    \"\"\"Return (is_ok, next_available_at). next_available_at is set when budget exceeded.\"\"\"\n    window_start = datetime.utcnow() - timedelta(hours=24)\n\n    # Global budget\n    global_sum_result = await db.execut…\n\n…e.fetched_at >= window_start,\n                ScrapeUsage.user_id == user_id,\n            )\n        )\n        oldest_at = oldest.scalar()\n        next_at = oldest_at + timedelta(hours=24) if oldest_at else datetime.utcnow() + timedelta(hours=24)\n        return False, next_at\n\n    return True, None\n\nasync def run_scrape_job(dataset_id: str, user_id: str):\n    \"\"\"Background task: scrape → sanitize → upsert → embed → themes → ready.\"\"\"\n    scraper = get_scraper()\n\n    async with AsyncSessionLocal() as db:\n        try:\n            dataset = await db.get(Dataset, dataset_id)\n            if not dataset:\n                logger.error(\"Dataset %s not found for scrape job\", dataset_id)\n                return\n\n            dataset.status = \"scraping\"\n            await db.commit()\n\n            # Submit job\n            try:\n                job_id = await asyncio.to_thread(scraper.submit_job, dataset.asin)\n            except Exception as exc:\n                logger.error(\"Failed to submit scrape job: %s\", exc)\n                dataset.status = \"error\"\n                dataset.error_message = f\"Failed to submit job: {exc}\"\n                await db.commi…\n\n### Backend RAG service (2)\nasync def index_dataset(dataset_id: str, db: AsyncSession):\n    \"\"\"Embed all reviews for a dataset and store in ChromaDB.\"\"\"\n    if not settings.OPENAI_API_KEY:\n        logger.info(\"No OPENAI_API_KEY — skipping ChromaDB indexing\")\n        return\n\n    result = await db.execute(\n        select(Review.id, Review.review_text).where(Review.dataset_id == dataset_id)\n    )\n    rows = result.fetchall()\n    if not rows:\n        return\n\n    collection = await asyncio.to_thread(_get_chroma_collection, dataset_id)\n\n    BATCH = 100\n    for i in range(0, len(rows), BATCH):\n        batch = rows[i : i + BATCH]\n        ids = [r[0] for r in batch]\n        texts = [r[1] for r in batch]\n        embeddings = await asyncio.to_thread(_embed_texts_sync, texts)\n        await asyncio.to_thread(\n            collection.upsert,\n            ids=ids,\n            embeddings=embeddings,\n            documents=texts,\n        )\n    logger.info(\"Indexed %d reviews for dataset %s\", len(rows), dataset_id)\n\ndef _keyword_search(dataset_id: str, query: str, top_k: int, all_texts: list[t…\n\n…glish\")\n        matrix = vectorizer.fit_transform(texts + [query])\n        sims = cosine_similarity(matrix[-1], matrix[:-1])[0]\n        top_indices = np.argsort(sims)[::-1][:top_k]\n        return [texts[i] for i in top_indices]\n    except Exception:\n        return [r[1] for r in all_texts[:top_k]]\n\nasync def retrieve_reviews(dataset_id: str, query: str, db: AsyncSession) -> list[str]:\n    \"\"\"Retrieve top-K relevant review texts for a query.\"\"\"\n    top_k = settings.RAG_TOP_K\n\n    if settings.OPENAI_API_KEY:\n        try:\n            collection = await asyncio.to_thread(_get_chroma_collection, dataset_id)\n            query_embedding = await asyncio.to_thread(_embed_texts_sync, [query])\n            results = await asyncio.to_thread(\n                collection.query,\n                query_embeddings=query_embedding,\n                n_results=min(top_k, collection.count()),\n            )\n            return results[\"documents\"][0] if results[\"documents\"] else []\n        except Exception as exc:\n            logge…\n\n### Backend RAG service (3)\ndef _mock_qa(message: str) -> str:\n    return (\n        \"Based on the provided reviews, customers generally have mixed opinions about this product. \"\n        \"Several reviewers mention positive aspects like quality and ease of use, while others note \"\n        \"concerns about battery life and durability. Please configure OPENAI_API_KEY for full AI responses.\"\n    )\n\nasync def run_qa(\n    dataset_id: str,\n    message: str,\n    history: list[dict],\n    db: AsyncSession,\n    user_id: str,\n) -> tuple[str, bool]:\n    \"\"\"Full RAG Q&A pipeline. Returns (reply, scope_refused).\"\"\"\n    start = time.time()\n    injection_detected = False\n\n    from app.security import input_guard, history_validator, output_guard\n\n    clean_message, msg_injection = input_guard.validate_message(message)\n    if msg_injection:\n        injection_detected = True\n\n    clean_history, hist_injection = history_validator.validate_history(history)\n    if hist_injection:\n        injection_detected = True\n\n    review_texts = await retrieve_reviews(dataset_id, clean_message, db)\n    review_context = _wrap_reviews(review_texts)\n\n    turn_count = len(clean_history) // 2 + 1\n    system_content = SYSTEM_PROMPT_TEMPLATE.format(review_context=review_context)\n    if turn_count > 0 and turn_count % settings.SYSTEM_REMINDER_INTERVAL_TURNS == 0:\n        system_content += SYSTEM_REMINDER\n\n    messages = [{\"role\": \"system\", \"content\": system_content}]\n    messages.extend(clean_history)\n    messages.append({\"role\": \"user\", \"content\": clean_message})\n\n    scope_refused = False\n\n    if not settings.OPENAI_API_KEY:\n        reply = _mock_qa(clean_message)\n    else:\n        try:\n            raw_reply = await asyncio.to_thread(_call_gpt4o_sync, messages)\n            reply = output_guard.filter_output(raw_reply)\n            scope_refused = await asyncio.to_thread(_classify_scope_sync, reply)\n        except ValueError:\n            reply = \"An error occurred processing the response. Please try again.\"\n            scope_refused = False\n        except Exception as exc:\n            logger.error(\"GPT-4o call failed: %s\", exc)\n            raise\n\n    latency_ms = int((time.time() - start) * 1000)\n\n    try:\n        AUDIT_LOG_PATH.parent.mkdir(parents=True, exist_ok=True)\n        with open(AUDIT_LOG_PATH, \"a\", encoding=\"utf-8\") as f:\n            entry = {\n                \"timestamp\": datetime.utcnow().isoformat() + \"Z\",\n                \"dataset_id\": dataset_id,\n                \"user_id\": user_id,\n                \"message_length\": len(message),\n                \"scope_refused\": scope_refused,\n                \"response_latency_ms\": latency_ms,\n                \"injection_pattern_detected\": injection_detected,\n            }\n            f.write(json.dumps(entry) + \"\\n\")\n    except Exception as exc:\n        logger.warning(\"Audit log write failed: %s\", exc)\n\n    return reply, scope_refused\n\n## password login timing attack constant time compare\n\n### Backend RAG service (3)\ndef _mock_qa(message: str) -> str:\n    return (\n        \"Based on the provided reviews, customers generally have mixed opinions about this product. \"\n        \"Several reviewers mention positive aspects like quality and ease of use, while others note \"\n        \"concerns about battery life and durability. Please configure OPENAI_API_KEY for full AI responses.\"\n    )\n\nasync def run_qa(\n    dataset_id: str,\n    message: str,\n    history: list[dict],\n    db: AsyncSession,\n    user_id: str,\n) -> tuple[str, bool]:\n    \"\"\"Full RAG Q&A pipeline. Returns (reply, scope_refused).\"\"\"\n    start = time.time()\n    injection_detected = False\n\n    from app.security import input_guard, history_validator, output_guard\n\n    clean_message, msg_injection = input_guard.validate_message(message)\n    if msg_injection:\n        injection_detected = True\n\n    clean_history, hist_injection = history_validator.validate_history(history)\n    if hist_injection:\n        injection_detected = True\n\n    review_texts = await retrieve_reviews(dataset_id, clean_message, db)\n    review_context = _wrap_reviews(review_texts)\n\n    turn_count = len(clean_history) // 2 + 1\n    system_content = SYSTEM_PROMPT_TEMPLATE.format(review_context=review_context)\n    if turn_count > 0 and turn_count % settings.SYSTEM_REMINDER_INTERVAL_TURNS == 0:\n        system_content += SYSTEM_REMINDER\n\n    messages = [{\"role\": \"system\", \"content\": system_content}]\n    messages.extend(clean_history)\n    messages.append({\"role\": \"user\", \"content\": clean_message})\n\n    scope_refused = False\n\n    if not settings.OPENAI_API_KEY:\n        reply = _mock_qa(clean_message)\n    else:\n        try:\n            raw_reply = await asyncio.to_thread(_call_gpt4o_sync, messages)\n            reply = output_guard.filter_output(raw_reply)\n            scope_refused = await asyncio.to_thread(_classify_scope_sync, reply)\n        except ValueError:\n            reply = \"An error occurred processing the response. Please try again.\"\n            scope_refused = False\n        except Exception as exc:\n            logger.error(\"GPT-4o call failed: %s\", exc)\n            raise\n\n    latency_ms = int((time.time() - start) * 1000)\n\n    try:\n        AUDIT_LOG_PATH.parent.mkdir(parents=True, exist_ok=True)\n        with open(AUDIT_LOG_PATH, \"a\", encoding=\"utf-8\") as f:\n            entry = {\n                \"timestamp\": datetime.utcnow().isoformat() + \"Z\",\n                \"dataset_id\": dataset_id,\n                \"user_id\": user_id,\n                \"message_length\": len(message),\n                \"scope_refused\": scope_refused,\n                \"response_latency_ms\": latency_ms,\n                \"injection_pattern_detected\": injection_detected,\n            }\n            f.write(json.dumps(entry) + \"\\n\")\n    except Exception as exc:\n        logger.warning(\"Audit log write failed: %s\", exc)\n\n    return reply, scope_refused\n\n### Backend RAG service (1)\n# Backend RAG service\n\n\"\"\"\nRAG pipeline for ReviewLens AI.\n\nRetrieval strategy: Embedding-based similarity via ChromaDB (cosine distance).\nEmbeddings generated by OpenAI text-embedding-3-small at ingest time and query time.\nFalls back to a simple keyword search when OpenAI API key is not configured.\n\"\"\"\n\nimport asyncio\nimport json\nimport logging\nimport os\nimport time\nfrom datetime import datetime\nfrom pathlib import Path\n\nfrom sqlalchemy import select\nfrom sqlalchemy.ext.asyncio import AsyncSession\n\nfrom app.config import settings\nfrom app.db.models import Review\n\nlogger = logging.getLogger(__name__)\n\nSYSTEM_PROMPT_TEMPLATE = \"\"\"\\\nYou are ReviewLens, an AI …\n\n### Frontend Login Page\n# Frontend Login Page\n\n'use client'\n\nimport { useState } from 'react'\nimport { useRouter } from 'next/navigation'\nimport { useAuth } from '@/lib/auth-context'\nimport { authApi } from '@/lib/api'\n\nexport default function LoginPage() {\n  const router = useRouter()\n  const { login } = useAuth()\n  const [email, setEmail] = useState('')\n  const [password, setPassword] = useState('')\n  const [error, setError] = useState('')\n  const [loading, setLoading] = useState(false)\n\n  const handleSubmit = async (e: React.FormEvent) => {\n    e.preventDefault()\n    setError('')\n    setLoading(true)\n    try {\n      const res = await authApi.login(email, password)\n      login(res.token, res.user)\n      router.push('/datasets')\n    } catch (err: any) {\n      setError(err?.data?.message || 'Invalid email or password.')\n    } finally {\n      setLoading(false)\n    }\n  }\n\n  return (\n    <div className=\"min-h-screen flex items-center justify-center bg-surface px-4\">\n      <div className=\"w-full max-w-sm\">\n        <div className=\"bg-white border border-border rounded-xl shadow-sm p-8\">\n          <div classN…\n\n…rder-border rounded-lg text-sm focus:outline-none focus:ring-2 focus:ring-accent focus:border-transparent\"\n                placeholder=\"analyst@firm.com\"\n              />\n            </div>\n\n            <div>\n              <label className=\"block text-sm font-medium text-text-primary mb-1\" htmlFor=\"password\">Password</label>\n              <input\n                id=\"password\"\n                type=\"password\"\n                autoComplete=\"current-password\"\n                required\n                value={password}\n                onChange={e => setPassword(e.target.value)}\n                className=\"w-full px-3 py-2 border border-border rounded-lg text-sm focus:outline-none focus:ring-2 focus:ring-accent focus:border-transparent\"\n              />\n            </div>\n\n            {error && (\n         …\n\n## BRIGHTDATA_DATASET_ID hardcoded gd_le8e811kzy4ggddlq\n\n### ── Bright Data ──────────────────────────────────────────────────────────────\n# ── Bright Data ──────────────────────────────────────────────────────────────\nBRIGHTDATA_API_KEY=1454c2f0-ce5e-4d6e-a1b5-ea602c185221\nBRIGHTDATA_DATASET_ID=gd_le8e811kzy4ggddlq\nBRIGHTDATA_MAX_REVIEWS_PER_ASIN=10000\nBRIGHTDATA_DAILY_REVIEW_LIMIT=10000\nBRIGHTDATA_PER_USER_DAILY_LIMIT=5000\n\n## database SQLite production concurrent writes WAL\n\n### ── App ──────────────────────────────────────────────────────────────────────\n# ── App ──────────────────────────────────────────────────────────────────────\nSECRET_KEY=change-me-to-a-random-secret-in-production\nDATABASE_URL=sqlite+aiosqlite:///./data/reviewlens.db\n\n### Backend RAG service (3)\ndef _mock_qa(message: str) -> str:\n    return (\n        \"Based on the provided reviews, customers generally have mixed opinions about this product. \"\n        \"Several reviewers mention positive aspects like quality and ease of use, while others note \"\n        \"concerns about battery life and durability. Please configure OPENAI_API_KEY for full AI responses.\"\n    )\n\nasync def run_qa(\n    dataset_id: str,\n    message: str,\n    history: list[dict],\n    db: AsyncSession,\n    user_id: str,\n) -> tuple[str, bool]:\n    \"\"\"Full RAG Q&A pipeline. Returns (reply, scope_refused).\"\"\"\n    start = time.time()\n    injection_detected = False\n\n    from app.security import input_guard, history_validator, output_guard\n\n    clean_message, msg_injection = input_guard.validate_message(message)\n    if msg_injection:\n        injection_detected = True\n\n    clean_history, hist_injection = history_validator.validate_history(history)\n    if hist_injection:\n        injection_detected = True\n\n    review_texts = await retrieve_reviews(dataset_id, clean_message, db)\n    review_context = _wrap_reviews(review_texts)\n\n    turn_count = len(clean_history) // 2 + 1\n    system_content = SYSTEM_PROMPT_TEMPLATE.format(review_context=review_context)\n    if turn_count > 0 and turn_count % settings.SYSTEM_REMINDER_INTERVAL_TURNS == 0:\n        system_content += SYSTEM_REMINDER\n\n    messages = [{\"role\": \"system\", \"content\": system_content}]\n    messages.extend(clean_history)\n    messages.append({\"role\": \"user\", \"content\": clean_message})\n\n    scope_refused = False\n\n    if not settings.OPENAI_API_KEY:\n        reply = _mock_qa(clean_message)\n    else:\n        try:\n            raw_reply = await asyncio.to_thread(_call_gpt4o_sync, messages)\n            reply = output_guard.filter_output(raw_reply)\n            scope_refused = await asyncio.to_thread(_classify_scope_sync, reply)\n        except ValueError:\n            reply = \"An error occurred processing the response. Please try again.\"\n            scope_refused = False\n        except Exception as exc:\n            logger.error(\"GPT-4o call failed: %s\", exc)\n            raise\n\n    latency_ms = int((time.time() - start) * 1000)\n\n    try:\n        AUDIT_LOG_PATH.parent.mkdir(parents=True, exist_ok=True)\n        with open(AUDIT_LOG_PATH, \"a\", encoding=\"utf-8\") as f:\n            entry = {\n                \"timestamp\": datetime.utcnow().isoformat() + \"Z\",\n                \"dataset_id\": dataset_id,\n                \"user_id\": user_id,\n                \"message_length\": len(message),\n                \"scope_refused\": scope_refused,\n                \"response_latency_ms\": latency_ms,\n                \"injection_pattern_detected\": injection_detected,\n            }\n            f.write(json.dumps(entry) + \"\\n\")\n    except Exception as exc:\n        logger.warning(\"Audit log write failed: %s\", exc)\n\n    return reply, scope_refused\n\n### Backend entrypoint\n# Backend entrypoint\n\n#!/bin/sh\nset -e\n\necho \"Running database migrations...\"\nalembic upgrade head\n\necho \"Starting server...\"\nexec uvicorn app.main:app --host 0.0.0.0 --port 8000\n\n## missing health check depends_on docker service\n\n### Backend ingest service (1)\n# Backend ingest service\n\nimport asyncio\nimport logging\nfrom datetime import datetime, timedelta\nfrom uuid import uuid4\n\nfrom sqlalchemy import func, select\nfrom sqlalchemy.ext.asyncio import AsyncSession\n\nfrom app.config import settings\nfrom app.db.models import Dataset, DatasetTheme, Review, ScrapeUsage\nfrom app.db.session import AsyncSessionLocal\nfrom app.security import blocklist\nfrom app.services.scraper import RawReview, get_scraper\n\nlogger = logging.getLogger(__name__)\n\nSCRAPE_TIMEOUT_SECONDS = 600  # 10 minutes\n\ndef _sentiment(star_rating: int) -> str:\n    if star_rating >= 4:\n        return \"positive\"\n    if star_rating == 3:\n        return \"neutral\"\n    return \"negative\"\n\ndef _sanitize(text: str) -> tuple[str, bool]:\n    truncated = text[: 2000]\n    cleaned, detected = blocklist.scan(truncated)\n    return cleaned, detected\n\nasync def check_budgets(db: AsyncSession, user_id: str) -> tuple[bool, datetime | None]:\n    \"\"\"Return (is_ok, next_available_at). next_available_at is set when budget exceeded.\"\"\"\n    window_start = datetime.utcnow() - timedelta(hours=24)\n\n    # Global budget\n    global_sum_result = await db.execute(\n       …\n\n…\"Failed to submit job: {exc}\"\n                await db.commit()\n                return\n\n            dataset.scrape_job_id = job_id\n            await db.commit()\n\n            # Poll with timeout\n            deadline = datetime.utcnow() + timedelta(seconds=SCRAPE_TIMEOUT_SECONDS)\n            from app.services.scraper import JobStatus\n            while True:\n                if datetime.utcnow() > deadline:\n                    dataset.status = \"error\"\n                    dataset.error_message = \"Scrape job timed out after 10 minutes.\"\n                    await db.commit()\n                    retur…\n\n### Backend ingest service (3)\n# Try to set product name from first review's context (mock sets asin-based)\n            if not dataset.product_name:\n                dataset.product_name = f\"Product {dataset.asin}\"\n                dataset.product_url = f\"https://www.amazon.com/dp/{dataset.asin}\"\n\n            await db.commit()\n\n            # Theme extraction\n            try:\n                from app.services.themes import extract_themes\n                all_texts_result = await db.execute(\n                    select(Review.review_text).where(Review.dataset_id == dataset_id)\n                )\n                texts = [row[0] for row in all_texts_result.fetchall()]\n                theme_pairs = await asyncio.to_thread(extract_themes, texts)\n\n                existing_themes = await db.execute(\n                    select(DatasetTheme).where(DatasetTheme.dataset_id == dataset_id)\n                )\n                for t in existing_themes.scalars().all():\n                    await db.delete(t)\n\n                for theme_text, freq in theme_pairs:\n                    db.add(DatasetTheme(\n                        id=str(uuid4()),\n                        dataset_id=dataset_id,\n                        theme=theme_text,\n                        frequency=freq,\n                    ))\n                await db.commit()\n            except Exception as exc:\n                logger.warning(\"Theme extraction failed: %s\", exc)\n\n            # Embeddings / ChromaDB\n            try:\n                from app.services.rag import index_dataset\n                await index_dataset(dataset_id, db)\n            except Exception as exc:\n                logger.warning(\"Embedding/indexing failed: %s\", exc)\n\n            dataset.status = \"ready\"\n            await db.commit()\n            logger.info(\"Dataset %s scrape complete: %d reviews\", dataset_id, len(raw_reviews))\n\n        except Exception as exc:\n            logger.exception(\"Unhandled error in scrape job for dataset %s: %s\", dataset_id, exc)\n            try:\n                dataset = await db.get(Dataset, dataset_id)\n                if dataset:\n                    dataset.status = \"error\"\n                    dataset.error_message = \"An unexpected error occurred during scraping.\"\n                    await db.commit()\n            except Exception:\n                pass\n\n### Backend RAG service (3)\ndef _mock_qa(message: str) -> str:\n    return (\n        \"Based on the provided reviews, customers generally have mixed opinions about this product. \"\n        \"Several reviewers mention positive aspects like quality and ease of use, while others note \"\n        \"concerns about battery life and durability. Please configure OPENAI_API_KEY for full AI responses.\"\n    )\n\nasync def run_qa(\n    dataset_id: str,\n    message: str,\n    history: list[dict],\n    db: AsyncSession,\n    user_id: str,\n) -> tuple[str, bool]:\n    \"\"\"Full RAG Q&A pipeline. Returns (reply, scope_refused).\"\"\"\n    start = time.time()\n    injection_detected = False\n\n    from app.security import input_guard, history_validator, output_guard\n\n    clean_message, msg_injection = input_guard.validate_message(message)\n    if msg_injection:\n        injection_detected = True\n\n    clean_history, hist_injection = history_validator.validate_history(history)\n    if hist_injection:\n        injection_detected = True\n\n    review_texts = await retrieve_reviews(dataset_id, clean_message, db)\n    review_context = _wrap_reviews(review_texts)\n\n    turn_count = len(clean_history) // 2 + 1\n    system_content = SYSTEM_PROMPT_TEMPLATE.format(review_context=review_context)\n    if turn_count > 0 and turn_count % settings.SYSTEM_REMINDER_INTERVAL_TURNS == 0:\n        system_content += SYSTEM_REMINDER\n\n    messages = [{\"role\": \"system\", \"content\": system_content}]\n    messages.extend(clean_history)\n    messages.append({\"role\": \"user\", \"content\": clean_message})\n\n    scope_refused = False\n\n    if not settings.OPENAI_API_KEY:\n        reply = _mock_qa(clean_message)\n    else:\n        try:\n            raw_reply = await asyncio.to_thread(_call_gpt4o_sync, messages)\n            reply = output_guard.filter_output(raw_reply)\n            scope_refused = await asyncio.to_thread(_classify_scope_sync, reply)\n        except ValueError:\n            reply = \"An error occurred processing the response. Please try again.\"\n            scope_refused = False\n        except Exception as exc:\n            logger.error(\"GPT-4o call failed: %s\", exc)\n            raise\n\n    latency_ms = int((time.time() - start) * 1000)\n\n    try:\n        AUDIT_LOG_PATH.parent.mkdir(parents=True, exist_ok=True)\n        with open(AUDIT_LOG_PATH, \"a\", encoding=\"utf-8\") as f:\n            entry = {\n                \"timestamp\": datetime.utcnow().isoformat() + \"Z\",\n                \"dataset_id\": dataset_id,\n                \"user_id\": user_id,\n                \"message_length\": len(message),\n                \"scope_refused\": scope_refused,\n                \"response_latency_ms\": latency_ms,\n                \"injection_pattern_detected\": injection_detected,\n            }\n            f.write(json.dumps(entry) + \"\\n\")\n    except Exception as exc:\n        logger.warning(\"Audit log write failed: %s\", exc)\n\n    return reply, scope_refused\n\n## input validation Pydantic email password length\n\n### Frontend Login Page\n…Frontend Login Page\n\n'use client'\n\nimport { useState } from 'react'\nimport { useRouter } from 'next/navigation'\nimport { useAuth } from '@/lib/auth-context'\nimport { authApi } from '@/lib/api'\n\nexport default function LoginPage() {\n  const router = useRouter()\n  const { login } = useAuth()\n  const [email, setEmail] = useState('')\n  const [password, setPassword] = useState('')\n  const [error, setError] = useState('')\n  const [loading, setLoading] = useState(false)\n\n  const handleSubmit = async (e: React.FormEvent) => {\n    e.preventDefault()\n    setError('')\n    setLoading(true)\n    try {\n      const res = await authApi.login(email, password)\n      login(res.token, res.user)\n      router.push('/datasets')\n    } catch (err: any) {\n      setError(err?.data?.message || 'Invalid email or password.')\n    } finally {\n      setLoading(false)\n    }\n  }\n\n  return (\n    <div className=\"min-h-screen flex items-center justify-center bg-surface px-4\">\n      <div className=\"w-full max-w-sm\">\n        <div className=\"bg-white border border-border rounded-xl shadow-sm p-8\">\n          <div classN…\n\n…nd\"/>\n            </svg>\n            <span className=\"text-xl font-semibold text-accent\">ReviewLens AI</span>\n          </div>\n\n          <form onSubmit={handleSubmit} className=\"space-y-4\">\n            <div>\n              <label className=\"block text-sm font-medium text-text-primary mb-1\" htmlFor=\"email\">Email</label>\n              <input\n                id=\"email\"\n                type=\"email\"\n                autoComplete=\"email\"\n                required\n                value={email}\n                onChange={e => setEmail(e.target.value)}\n                className=\"w-full px-3 py-2 border border-border rounded-lg text-sm focus:outline-none focus:ring-2 focus:ring-accent focus:border-transparent\"\n                placeholder=\"analyst@firm.com\"\n              />\n            </div>\n\n            <div>\n              <label className=\"block text-sm font-medium text-text-primary mb-1\" htmlFor=\"password\">Password</label>\n              <input\n                id=\"password\"\n                type=\"password\"\n                autoComplete=\"current-password\"\n                required\n                value={password}\n                onChange={e => setPassword(e.target.value)}\n                className=\"w-full px-3 py-2 border border-border rounded-lg text-sm focus:outline-none focus:ring-2 focus:ring-accent focus:border-transparent\"\n              />\n            </div>\n\n            {error && (\n         …\n\n### Backend RAG service (3)\ndef _mock_qa(message: str) -> str:\n    return (\n        \"Based on the provided reviews, customers generally have mixed opinions about this product. \"\n        \"Several reviewers mention positive aspects like quality and ease of use, while others note \"\n        \"concerns about battery life and durability. Please configure OPENAI_API_KEY for full AI responses.\"\n    )\n\nasync def run_qa(\n    dataset_id: str,\n    message: str,\n    history: list[dict],\n    db: AsyncSession,\n    user_id: str,\n) -> tuple[str, bool]:\n    \"\"\"Full RAG Q&A pipeline. Returns (reply, scope_refused).\"\"\"\n    start = time.time()\n    injection_detected = False\n\n    from app.security import input_guard, history_validator, output_guard\n\n    clean_message, msg_injection = input_guard.validate_message(message)\n    if msg_injection:\n        injection_detected = True\n\n    clean_history, hist_injection = history_validator.validate_history(history)\n    if hist_injection:\n        injection_detected = True\n\n    review_texts = await retrieve_reviews(dataset_id, clean_message, db)\n    review_context = _wrap_reviews(review_texts)\n\n    turn_count = len(clean_history) // 2 + 1\n    system_content = SYSTEM_PROMPT_TEMPLATE.format(review_context=review_context)\n    if turn_count > 0 and turn_count % settings.SYSTEM_REMINDER_INTERVAL_TURNS == 0:\n        system_content += SYSTEM_REMINDER\n\n    messages = [{\"role\": \"system\", \"content\": system_content}]\n    messages.extend(clean_history)\n    messages.append({\"role\": \"user\", \"content\": clean_message})\n\n    scope_refused = False\n\n    if not settings.OPENAI_API_KEY:\n        reply = _mock_qa(clean_message)\n    else:\n        try:\n            raw_reply = await asyncio.to_thread(_call_gpt4o_sync, messages)\n            reply = output_guard.filter_output(raw_reply)\n            scope_refused = await asyncio.to_thread(_classify_scope_sync, reply)\n        except ValueError:\n            reply = \"An error occurred processing the response. Please try again.\"\n            scope_refused = False\n        except Exception as exc:\n            logger.error(\"GPT-4o call failed: %s\", exc)\n            raise\n\n    latency_ms = int((time.time() - start) * 1000)\n\n    try:\n        AUDIT_LOG_PATH.parent.mkdir(parents=True, exist_ok=True)\n        with open(AUDIT_LOG_PATH, \"a\", encoding=\"utf-8\") as f:\n            entry = {\n                \"timestamp\": datetime.utcnow().isoformat() + \"Z\",\n                \"dataset_id\": dataset_id,\n                \"user_id\": user_id,\n                \"message_length\": len(message),\n                \"scope_refused\": scope_refused,\n                \"response_latency_ms\": latency_ms,\n                \"injection_pattern_detected\": injection_detected,\n            }\n            f.write(json.dumps(entry) + \"\\n\")\n    except Exception as exc:\n        logger.warning(\"Audit log write failed: %s\", exc)\n\n    return reply, scope_refused\n\n### Frontend API lib\n…nst err = await res.json().catch(() => ({ message: 'Request failed' }))\n    throw Object.assign(new Error(err.message || 'Request failed'), { status: res.status, data: err })\n  }\n\n  if (res.status === 204) return undefined as T\n  return res.json()\n}\n\n// Auth\nexport const authApi = {\n  login: async (email: string, password: string) => {\n    const res = await fetch(`${API_BASE}/auth/login`, {\n      method: 'POST',\n      headers: { 'Content-Type': 'application/json' },\n      body: JSON.stringify({ email, password }),\n    })\n    if (!res.ok) {\n      const err = await res.json().catch(() => ({ message: 'Login failed' }))\n      throw Object.assign(new Error(err.message || 'Login failed'), { status: res.status, data: err })\n    }\n    return res.json() as Promise<{ token: string; expires_at: string; user: { id: string; email: string } }>\n  },\n  logout: () => apiFetch<void>('/auth/logout', { method: 'POST' }),\n}\n\n// Datasets\nexport const datasetsApi = {\n  list: () => apiFetch<{ datasets: Dataset[] }>('/datasets'),\n  get: (id: string) => apiFetch<Dataset>(`/datasets/${id}`),\n  create: (input: string) =>\n    apiFetch<{ id: string; asin: string; status: string }>('/datasets', {\n      method: 'POST',\n      body: JSON.stringify({ input }),\n    }),\n  upload: async (file: File): Promise<{ id: string; status: string }> => {\n    const token = getToken()\n    const headers: Record<string, string> = {}\n    if (token) headers['Authorization'] = `Bearer ${token}`\n    const body = new FormData()\n    body.append('file', file)\n    const…\n\n## fetch error response json parse error handling frontend\n\n### Frontend Auth Context\n# Frontend Auth Context\n\n'use client'\n\nimport { createContext, useContext, useEffect, useState, ReactNode } from 'react'\nimport { User } from '@/types'\n\ninterface AuthContextValue {\n  token: string | null\n  user: User | null\n  login: (token: string, user: User) => void\n  logout: () => void\n  isAuthenticated: boolean\n  initialized: boolean\n}\n\nconst AuthContext = createContext<AuthContextValue | null>(null)\n\nexport function AuthProvider({ children }: { children: ReactNode }) {\n  const [token, setToken] = useState<string | null>(null)\n  const [user, setUser] = useState<User | null>(null)\n  const [initialized, setInitialized] = useState(false)\n\n  useEffect(() => {\n    const savedToken = sessionStorage.getItem('rl_token')\n    const savedUser = sessionStorage.getItem('rl_user')\n    if (savedToken && savedUser) {\n      setToken(savedToken)\n      setUser(JSON.parse(savedUser))\n    }\n    setInitialized(true)\n  }, [])\n\n  const login = (newToken: string, newUser: User) => {\n    sessionStorage.setItem('rl_token', newToken)\n    sessionStorage.setItem('rl_user', JSON.stringify(newUser))\n    setToken(newToken)\n    setUser(newUser)\n  }\n\n  const logout = () => {\n    sessionStorage.removeItem('rl_token')\n    sessionStorage.removeItem('rl_user')\n    setToken(null)\n    setUser(null)\n  }\n\n  return (\n    <AuthContext.Provider value={{ token, user, login, logout, isAuthenticated: !!token, initialized }}>\n      {children}\n    </AuthContext.Provider>\n  )\n}\n\nexport function useAuth() {\n  const ctx = useContext(AuthContext)\n  if (!ctx) throw new Error('useAuth must be used within AuthProvider')\n  return ctx\n}\n\n### Frontend API lib\n# Frontend API lib\n\nimport { Dataset, Review, ReviewsResponse, Summary, RateLimitStatus, ChatMessage } from '@/types'\n\nconst API_BASE = '/api/v1'\n\nfunction getToken(): string | null {\n  if (typeof window === 'undefined') return null\n  return sessionStorage.getItem('rl_token')\n}\n\nasync function apiFetch<T>(path: string, options: RequestInit = {}): Promise<T> {\n  const token = getToken()\n  const headers: Record<string, string> = {\n    'Content-Type': 'application/json',\n    ...(options.headers as Record<string, string> || {}),\n  }\n  if (token) headers['Authorization'] = `Bearer ${token}`\n\n  const res = await fetch(`${API_BASE}${path}`, { ...options, headers })\n\n  if (res.status === 401) {\n    sessionStorage.removeItem('rl_token')\n    sessionStorage.removeItem('rl_user')\n    window.location.href = '/login?expired=1'\n    throw new Error('Unauthorized')\n  }\n\n  if (!res.ok) {\n    const err = await res.json().catch(() => ({ message: 'Request failed' }))\n    throw Object.assign(new Error(err.message || 'Request failed'), { status: res.status, data: err })\n  }\n\n  if (res.status === 204) return undefined as T\n  return res.json()\n}\n\n// Auth\nexport const authApi = {\n  login: async (email: string, password: string) => {\n    const res = await fetch(`${API_BASE}/auth/login`, {\n      method: 'POST',\n      headers: { 'Content-Type': 'application/json' },\n      body: JSON.stringify({ email, password }),\n    })\n    if (!res.ok) {\n      const err = await res.json().catch(() => ({ message: 'Login failed' }))\n      throw Object.assign(new Error(err.message || 'Login failed'), { status: res.status, data: err })\n    }\n    return res.json() as Promise<{ token: string; expires_at: string; user: { id: string; email: string } }>\n  },\n  logout: () => apiFetch<void>('/auth/logout', { method: 'POST' }),\n}\n\n// Datasets\nexport const datasetsApi = {\n  list: () => apiFetch<{ datasets: Dataset[] }>('/datasets'),\n  get: (id: string) => apiFetch<Dataset>(`/datasets/${id}`),\n  create: (input: string) =>\n    apiFetch<{ id: string; asin: string; status: string }>('/datasets', {\n      method: 'POST',\n      body: JSON.stringify({ input }),\n    }),\n  upload: async (file: File): Promise<{ id: string; status: string }> => {\n    const token = getToken()\n    const headers: Record<string, string> = {}\n    if (token) headers['Authorization'] = `Bearer ${token}`\n    const body = new FormData()\n    body.append('file', file)\n    const res = await fetch(`${API_BASE}/datasets/upload`, { method: 'POST', headers, body })\n    if (res.status === 401) {\n      sessionStorage.removeItem('rl_token')\n      sessionStorage.removeItem('rl_user')\n      window.location.href = '/login?expired=1'\n      throw new Error('Unauthorized')\n    }\n    if (!res.ok) {\n      const err = await res.json().catch(() => ({ message: 'Upload failed' }))\n      throw Object.assign(new Error(err.message || 'Upload failed'), { status: res.status, data: err })\n    }\n    return res.json()\n  },\n  rescrape: (id: string) =>\n    apiFetch<{ sta…\n\n### Backend RAG service (3)\ndef _mock_qa(message: str) -> str:\n    return (\n        \"Based on the provided reviews, customers generally have mixed opinions about this product. \"\n        \"Several reviewers mention positive aspects like quality and ease of use, while others note \"\n        \"concerns about battery life and durability. Please configure OPENAI_API_KEY for full AI responses.\"\n    )\n\nasync def run_qa(\n    dataset_id: str,\n    message: str,\n    history: list[dict],\n    db: AsyncSession,\n    user_id: str,\n) -> tuple[str, bool]:\n    \"\"\"Full RAG Q&A pipeline. Returns (reply, scope_refused).\"\"\"\n    start = time.time()\n    injection_detected = False\n\n    from app.security import input_guard, history_validator, output_guard\n\n    clean_message, msg_injection = input_guard.validate_message(message)\n    if msg_injection:\n        injection_detected = True\n\n    clean_history, hist_injection = history_validator.validate_history(history)\n    if hist_injection:\n        injection_detected = True\n\n    review_texts = await retrieve_reviews(dataset_id, clean_message, db)\n    review_context = _wrap_reviews(review_texts)\n\n    turn_count = len(clean_history) // 2 + 1\n    system_content = SYSTEM_PROMPT_TEMPLATE.format(review_context=review_context)\n    if turn_count > 0 and turn_count % settings.SYSTEM_REMINDER_INTERVAL_TURNS == 0:\n        system_content += SYSTEM_REMINDER\n\n    messages = [{\"role\": \"system\", \"content\": system_content}]\n    messages.extend(clean_history)\n    messages.append({\"role\": \"user\", \"content\": clean_message})\n\n    scope_refused = False\n\n    if not settings.OPENAI_API_KEY:\n        reply = _mock_qa(clean_message)\n    else:\n        try:\n            raw_reply = await asyncio.to_thread(_call_gpt4o_sync, messages)\n            reply = output_guard.filter_output(raw_reply)\n            scope_refused = await asyncio.to_thread(_classify_scope_sync, reply)\n        except ValueError:\n            reply = \"An error occurred processing the response. Please try again.\"\n            scope_refused = False\n        except Exception as exc:\n            logger.error(\"GPT-4o call failed: %s\", exc)\n            raise\n\n    latency_ms = int((time.time() - start) * 1000)\n\n    try:\n        AUDIT_LOG_PATH.parent.mkdir(parents=True, exist_ok=True)\n        with open(AUDIT_LOG_PATH, \"a\", encoding=\"utf-8\") as f:\n            entry = {\n                \"timestamp\": datetime.utcnow().isoformat() + \"Z\",\n                \"dataset_id\": dataset_id,\n                \"user_id\": user_id,\n                \"message_length\": len(message),\n                \"scope_refused\": scope_refused,\n                \"response_latency_ms\": latency_ms,\n                \"injection_pattern_detected\": injection_detected,\n            }\n            f.write(json.dumps(entry) + \"\\n\")\n    except Exception as exc:\n        logger.warning(\"Audit log write failed: %s\", exc)\n\n    return reply, scope_refused\n\n## SECRET_KEY dev-secret hardcoded default insecure\n\n### ── App ──────────────────────────────────────────────────────────────────────\n# ── App ──────────────────────────────────────────────────────────────────────\nSECRET_KEY=change-me-to-a-random-secret-in-production\nDATABASE_URL=sqlite+aiosqlite:///./data/reviewlens.db\n\n### Frontend Chat Page (1)\n…    <div className=\"max-w-[75%] bg-surface border border-border rounded-xl rounded-tl-sm px-4 py-3 text-sm shadow-sm\">\n        <div className=\"text-xs text-text-secondary mb-1\">ReviewLens AI</div>\n        <div className=\"whitespace-pre-wrap\">{msg.content}</div>\n      </div>\n    </div>\n  )\n}\n\nexport default function ChatPage() {\n  const { id } = useParams<{ id: string }>()\n  const [messages, setMessages] = useState<ChatMessage[]>([])\n  const [input, setInput] = useState('')\n  const [sending, setSending] = useState(false)\n  const [error, setError] = useState('')\n  const bottomRef = useRef<HTMLDi…\n\n### Frontend Login Page\n# Frontend Login Page\n\n'use client'\n\nimport { useState } from 'react'\nimport { useRouter } from 'next/navigation'\nimport { useAuth } from '@/lib/auth-context'\nimport { authApi } from '@/lib/api'\n\nexport default function LoginPage() {\n  const router = useRouter()\n  const { login } = useAuth()\n  const [email, setEmail] = useState('')\n  const [password, setPassword] = useState('')\n  const [error, setError] = useState('')\n  const [loading, setLoading] = useState(false)\n\n  const handleSubmit = async (e:…\n\n\nSearchable terms for follow-up: review_date, reviewer_name, verified_purchase, last_scraped_at, avg_star_rating, review_date_min, review_date_max, _sentiment, reviews_fetched, fetched_at, brightdata_daily_review_limit, brightdata_per_user_daily_limit, raw_reviews, brightdata_max_reviews_per_asin, index_dataset, system_prompt_template, review_context, system_reminder, scope_classifier_sys, scope_classifier_user, audit_log_path, _get_chroma_collection, chroma_persist_dir, _get_openai_client, _embed_texts_sync, retrieve_reviews, _wrap_reviews, _call_gpt4o_sync, _classify_scope_sync, system_reminder_interval_turns, date_desc, _sanitize, rag_top_k, rl_token, per_page, __name__, rl_user, job_id, scope_refused, product_name"
  }
]